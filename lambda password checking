# lamba function password checking
import re

# Single expression lambda
is_valid = lambda p: len(p) >= 8 and re.search(r"[A-Z]", p) and re.search(r"[a-z]", p) and re.search(r"[0-9]", p)

password = input("Enter password: ")

print("Valid password" if is_valid(password) else "Invalid password")

