n = int(input("Enter the number: "))
total = 0

for i in range(1, n+1):
    num = 1
    max_in_row = 1
    for j in range(1, i):
        num = num * (i - j) // j
        if num > max_in_row:
            max_in_row = num
    total += max_in_row

print("Sum of largest numbers:", total)
